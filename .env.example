# Backend (FastAPI) — copy to `.env` in repo root if you use a loader, or export in shell.
# Defaults in main.py match local development.

# Ollama HTTP API (must include /api/generate)
OLLAMA_URL=http://localhost:11434/api/generate
